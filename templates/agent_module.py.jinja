{# agent_module.py.jinja — Template de modulo de agente #}
# 3W: WHAT=agente {{ agent_name }} | WHY=orquestracao LLM | WHEN={{ trace }}
"""
Agente: {{ agent_name }}
Modelo: {{ model }}
Perfil: {{ profile }}
Gerado por: factory_engine.py (AST + Jinja2)
Trace: {{ trace }}
"""
from pathlib import Path

BUILD = Path(__file__).parent.parent

# ═══════════════════════════════════════════════════════════
# Configuracao do agente
# ═══════════════════════════════════════════════════════════

AGENT_ID = "{{ agent_name }}"
MODEL = "{{ model }}"
PROFILE = "{{ profile }}"

{% include "partials/agent_init.py.jinja" %}

{% include "partials/agent_run.py.jinja" %}

# ═══════════════════════════════════════════════════════════
# Entry point
# ═══════════════════════════════════════════════════════════

if __name__ == "__main__":
    import sys
    prompt = sys.argv[1] if len(sys.argv) > 1 else "Hello."
    result = run(prompt)
    print(result)
