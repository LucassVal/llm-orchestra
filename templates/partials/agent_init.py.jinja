{# partials/agent_init.py.jinja — inicializacao do agente #}
def init():
    """Inicializa agente com configuracao do perfil."""
    return {
        "agent_id": AGENT_ID,
        "model": MODEL,
        "profile": PROFILE,
    }
